#!/bin/sh
# Maven wrapper shell script
MAVEN_WRAPPER_JAR=".mvn/wrapper/maven-wrapper.jar"
MAVEN_OPTS="${MAVEN_OPTS:-}"

if [ -r "$MAVEN_WRAPPER_JAR" ]; then
  exec java $MAVEN_OPTS -jar "$MAVEN_WRAPPER_JAR" "$@"
else
  exec mvn "$@"
fi
